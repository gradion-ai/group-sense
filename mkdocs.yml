site_name: Group Sense
site_description: Multi-party conversation intelligence for AI agents
site_url: https://gradion-ai.github.io/group-sense/

repo_name: gradion-ai/group-sense
repo_url: https://github.com/gradion-ai/group-sense

copyright: Copyright &copy; 2025 Gradion AI

theme:
  name: material
  font:
    text: Fira
    code: Fira Code
  palette:
  - media: "(prefers-color-scheme)"
    scheme: default
    primary: indigo
    accent: indigo
    toggle:
      icon: material/link
      name: Switch to light mode
  - media: "(prefers-color-scheme: light)"
    scheme: default
    primary: indigo
    accent: indigo
    toggle:
      icon: material/toggle-switch
      name: Switch to dark mode
  - media: "(prefers-color-scheme: dark)"
    scheme: slate
    primary: black
    accent: indigo
    toggle:
      icon: material/toggle-switch-off
      name: Switch to system preference
  features:
    - navigation.tabs
    - navigation.sections
    - navigation.instant
    - navigation.instant.prefetch
    - navigation.instant.preview
    - navigation.instant.progress
    - navigation.top
    - navigation.tracking
    - search.suggest
    - toc.follow
    - content.code.copy
    - content.code.select
    - content.code.annotate

plugins:
  - search
  - mkdocstrings:
      default_handler: python
      handlers:
        python:
          paths: [group_sense]
          options:
            show_root_heading: true
            show_source: false
            show_signature_annotations: true
            signature_crossrefs: true
            separate_signature: true
            merge_init_into_class: true
            extensions:
              - griffe_pydantic
          inventories:
            - url: https://docs.python.org/3/objects.inv
              domains: [py, std]
  - llmstxt:
      markdown_description: |
        The `group_sense` package is a library for detecting patterns in group chat message streams
        and transforming them into self-contained queries for downstream AI systems. This enables
        existing single-user AI agents to participate in group conversations based on configurable
        criteria, without requiring training on multi-party conversations.
      full_output: llms-full.txt
      sections:
        User Guide:
          - index.md: Introduction to Group Sense
          - installation.md: Installation and API key setup
          - basics.md: Core concepts and reasoner types
          - examples.md: Usage examples for different engagement patterns
          - integration.md: Integrating Group Sense into applications
        API Documentation:
          - api/message.md: Message data structures
          - api/reasoner.md: Reasoner interfaces and implementations

markdown_extensions:
  - pymdownx.highlight:
      anchor_linenums: true
      line_spans: __span
      pygments_lang_class: true
  - pymdownx.inlinehilite
  - pymdownx.snippets:
      dedent_subsections: true
  - pymdownx.superfences
  - pymdownx.details
  - attr_list
  - md_in_html
  - admonition
  - pymdownx.emoji:
      emoji_index: !!python/name:material.extensions.emoji.twemoji
      emoji_generator: !!python/name:material.extensions.emoji.to_svg

nav:
  - User Guide:
    - Introduction: index.md
    - Installation: installation.md
    - Basics: basics.md
    - Examples: examples.md
    - Integration: integration.md
  - API Documentation:
    - Message: api/message.md
    - Reasoner: api/reasoner.md
